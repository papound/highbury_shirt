# Project Rules

- DO NOT push to Git unless explicitly ordered by the user. Committing changes locally is fine.
- When ordered to push, always run `npm run build` first to ensure there are no production build errors.
